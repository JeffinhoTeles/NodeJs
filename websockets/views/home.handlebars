<h1>Lista de Produtos (Estática)</h1>
<ul>
    {{#each products}}
        <li>{{this.title}} - {{this.price}}</li>
    {{/each}}
</ul>

<a href="/realtimeproducts">Ir para a visualização em tempo real</a>
